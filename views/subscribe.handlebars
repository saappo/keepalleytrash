<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow">
            <div class="card-header bg-success text-white text-center">
                <h4 class="mb-0">
                    <i class="fas fa-envelope me-2"></i>Subscribe to Our Newsletter
                </h4>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <h6><i class="fas fa-info-circle me-2"></i>Newsletter Information</h6>
                    <p class="mb-0">
                        <strong>Important:</strong> When you register for an account, you are automatically subscribed to our newsletter. 
                        This form is for additional email addresses or for those who want to subscribe without creating an account.
                    </p>
                </div>
                
                <p class="lead text-center mb-4">Stay updated with the latest community news, cleanup events, and neighborhood improvements!</p>
                
                {{#if errors}}
                    {{#each errors}}
                        <div class="alert alert-danger">{{msg}}</div>
                    {{/each}}
                {{/if}}
                
                <form method="POST" action="/subscribe">
                    <div class="mb-3">
                        <label for="email" class="form-label">Email Address</label>
                        <input type="email" class="form-control" id="email" name="email" value="{{formData.email}}" placeholder="Enter your email address" required>
                        <div class="form-text">
                            <i class="fas fa-shield-alt me-1"></i>
                            We respect your privacy. Your email will only be used for newsletter communications.
                        </div>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-success btn-lg">
                            <i class="fas fa-paper-plane me-2"></i>Subscribe to Newsletter
                        </button>
                    </div>
                </form>
                
                <hr>
                
                <div class="row text-center">
                    <div class="col-md-4">
                        <i class="fas fa-bell fa-2x text-success mb-2"></i>
                        <h6>Stay Informed</h6>
                        <p class="small text-muted">Get notified about important updates</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-calendar fa-2x text-success mb-2"></i>
                        <h6>Event Reminders</h6>
                        <p class="small text-muted">Never miss a cleanup event</p>
                    </div>
                    <div class="col-md-4">
                        <i class="fas fa-users fa-2x text-success mb-2"></i>
                        <h6>Community News</h6>
                        <p class="small text-muted">Latest neighborhood happenings</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 